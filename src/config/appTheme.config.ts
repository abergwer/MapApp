/**
 * Re-exports design system theme mode for backwards-compatible imports.
 * Prefer `designSystem.config.ts` as the hub.
 */
export {
  designSystemConfig,
  appThemeConfig,
  type AppThemeMode,
  type DesignSystemConfig,
} from './designSystem.config';
